# ------------------------------------------------------

# Docker Compose automatically loads .env at runtime.

# Any value not overridden keeps the default baked into the code.

# ---- Downloader -------------------------------------------------

URL_FILE=/data/image_urls.txt         # path inside container
OUTPUT_DIR=/data/images               # shared volume mount
MAX_CONCURRENCY=32                    # parallel HTTP sockets
POLL_SECONDS=30                       # re‑scan interval (seconds)

# ---- Embedder ---------------------------------------------------

IMAGES_DIR=/data/images               # where to look for JPEG/PNG
MODEL=RN50                             # OpenCLIP checkpoint name
LOG_LEVEL=INFO                         # DEBUG | INFO | WARNING
POLL_SECONDS=30                        # sync with downloader

# ---- Elasticsearch ---------------------------------------------

ES_HOST=http://es:9200                # vector store URL

# HNSW index parameters (quality vs. size). Override only if you rebuild
HNSW_M=16       # number of links per node
HNSW_EF=512     # ef_construction (index build breadth)

# ---- Search API -------------------------------------------------

# (inherits ES_HOST & IMAGES_DIR)

API_PORT=8000                          # host port mapping if you change compose

# ---- UI ---------------------------------------------------------

STREAMLIT_SERVER_PORT=8501             # host port if you customise compose

# ---- Python runtime --------------------------------------------

PYTHONUNBUFFERED=1                     # flush logs immediately